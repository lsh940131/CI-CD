version: '3.8'
services:
  app:
    container_name: 'cicd-container'
    build:
      context: .
      dockerfile: ./Dockerfile.prod
    ports:
      - 80:3000
    volumes:
      - ./src:/usr/app/src
